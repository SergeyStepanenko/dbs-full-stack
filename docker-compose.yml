version: "3"
services:
  backend:
    container_name: dbs_node
    restart: always
    build: backend
    ports:
      - "5000:5000"
    links:
      - mongo
  mongo:
    container_name: dbs_mongo
    image: mongo
    ports:
      - "27017:27017"
